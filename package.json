{
  "name": "loon-app-ip-router",
  "version": "1.1.0",
  "private": true,
  "description": "Loon App traffic observer and human-confirmed routing rule generator",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "simulate": "node scripts/simulate.js",
    "verify": "npm run build && npm test && npm run simulate"
  },
  "engines": {
    "node": ">=18"
  }
}
